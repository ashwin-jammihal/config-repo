name: default
age: 0
hobbies:
  name1: 12
  name2: 12
security:
  user: defaultusername
  password: defaultpass
  roles:
    - USER
    - ADMIN
    - POWERUSER
hystrix:
  threadpool:
    maxPoolSize: 40
    maxQueuSize: 39
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, httptrace

  endpoint:
    health:
      show-details: always
